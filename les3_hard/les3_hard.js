let str = "урок-3-был слишком легким";
let uper = str.slice(0,1).toUpperCase() + str.slice(1, );
let repl = uper.replace(/-/g,' ');
console.log(repl);
let light = repl.substring(19, ).replace('им','о');
console.log(light);
